services:
  nginx:
    image: nginx:alpine
    container_name: plavdata_nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/templates/nginx.conf.template:ro
      - ./certbot/www:/var/www/certbot:ro
      - ./certbot/conf:/etc/letsencrypt:ro
      - frontend_dist:/usr/share/nginx/html:ro
    environment:
      - SERVER_NAME=${SERVER_NAME:-localhost}
    networks:
      - app-network
    depends_on:
      - backend
      - frontend
    restart: unless-stopped
    command: >
      /bin/sh -c "envsubst '$$SERVER_NAME' < /etc/nginx/templates/nginx.conf.template > /etc/nginx/nginx.conf && nginx -g 'daemon off;'"

  certbot:
    image: certbot/certbot
    container_name: plavdata_certbot
    volumes:
      - ./certbot/www:/var/www/certbot:rw
      - ./certbot/conf:/etc/letsencrypt:rw
    depends_on:
      - nginx

  backend:
    build:
      context: ./backend
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    container_name: plavdata_be
    user: "${UID:-1000}:${GID:-1000}"
    networks:
      - app-network
    expose:
      - "8000"
    volumes:
      - "./backend/data:/app/data"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - ENV=production
      - CORS_ORIGINS=${CORS_ORIGINS:-}
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL}
    container_name: plavdata_fe
    volumes:
      - frontend_dist:/output_volume
    entrypoint: >
      /bin/sh -c "
       cp -r /app/dist/. /output_volume &&
       tail -f /dev/null
       "
    restart: unless-stopped

networks:
  app-network:
    driver: bridge

volumes:
  frontend_dist:

